comment:
  layout: "flags, diff, files"
  behavior: default
  require_base: no
  require_head: no
  require_changes: false  
  require_base: no
  require_head: no
  show_carryforward_flags: true

flag_management:
  default_rules:
    carryforward: true